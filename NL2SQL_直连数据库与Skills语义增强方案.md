# NL2SQL：直连数据库与 Skills 语义增强方案

## 1. 背景与问题

在自然语言转 SQL 场景中，常见做法是直接连接数据库，通过 Schema（表、字段、关系）让模型生成 SQL。  
这种方式有明显优势：实时、可执行、可验证。

但实际生产中存在一个关键问题：

- 很多数据库缺少 `table comment` / `column comment`
- 字段名可能是缩写、历史命名或业务黑话
- 仅靠字段名翻译，容易误解字段真实业务含义

因此，单纯依赖数据库 Schema 往往不够，尤其在复杂业务系统中。

## 2. 核心结论

最佳路线不是二选一，而是：

- 以 **直连数据库** 作为主能力（事实数据来源）
- 以 **Skills 文档** 作为语义增强层（业务语义来源）

即：`Schema 负责“能查”`，`Skills 负责“查对”`。

## 3. 为什么直连数据库仍然是主轴

- 实时性：查询的是当前生产数据，不依赖离线快照
- 可验证性：SQL 可以真实执行并回传结果
- 可扩展性：可接入 MySQL/PostgreSQL/SQL Server 等多种数据源
- 工程可控：可叠加权限、审计、限流、风险拦截

## 4. 为什么 Skills 必须保留

Skills 解决的是 Schema 无法稳定覆盖的“业务语义缺口”：

- 表的真实业务用途（不是表名字面意思）
- 字段业务定义与口径说明（例如“新增用户”的定义）
- 组织内部约定（黑名单字段、默认过滤条件、时间口径）

这部分知识通常不在数据库注释里，或注释不完整、不可信。

## 5. 推荐语义解析优先级

面对用户问题时，建议按以下优先级融合语义：

1. 数据库结构事实：表结构、主外键、索引等
2. 数据库注释信息：表注释、字段注释（若存在）
3. Skills 语义文档：业务定义、指标口径、字段解释
4. 弱推断信号：命名规则、样本值、关联模式（仅辅助）

说明：

- 前两层偏“技术事实”
- 后两层偏“业务语义”
- 生成 SQL 时应显式记录使用了哪些语义来源

## 6. 可信度机制（强烈建议）

为避免“看起来很对，实际答错”的风险，建议引入字段语义可信度：

- `high`：来自注释或高质量 skills，含义明确
- `medium`：来自命名规则或部分上下文，基本可用
- `low`：仅弱推断，含义不稳定

落地策略：

- 若 SQL 依赖 `low` 字段，先提示用户确认口径
- 对高风险查询返回“需要确认”的解释信息
- 在结果页显示本次回答的语义可信度

## 7. 生产落地原则

- 默认只开放 `SELECT`
- 禁止或审批 `UPDATE/DELETE/INSERT`
- 增加 SQL 风险检测（全表扫描、跨库大查询、敏感字段）
- 全链路审计（用户问题、生成 SQL、执行时间、结果摘要）
- 对外输出不止 SQL：还应包含表格、图表、结论摘要

## 8. 产品定位建议

不要把系统定位成“SQL 生成器”，而应定位为：

**自然语言数据分析助手（Natural Language BI Assistant）**

其核心价值是：

- 理解业务问题
- 生成并安全执行 SQL
- 输出可直接决策的结果（图表 + 解释）

## 9. 一句话总结

直连数据库解决“能查到数据”，Skills 解决“查到的是不是业务真正要的答案”。  
两者结合，才是可在生产场景稳定落地的 NL2SQL 方案。
